{
    "configurations": [
        {
            "name": "(gdb) Launch",
            "stopAtEntry": true,
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/debug/app.exe",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "cwd": "${workspaceFolder}/bin/debug/",
            "preLaunchTask": "c++ all files clang debug",
            "logging": {
                "engineLogging": true,
                "trace": true,
                "traceResponse": true
            }
        },
    ]
}